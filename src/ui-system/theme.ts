import { MD3DarkTheme, MD3LightTheme, type MD3Theme } from 'react-native-paper';

/** Tokens MD3 de la app. Se ajustan acá y no en cada pantalla. */
export const lightTheme: MD3Theme = { ...MD3LightTheme, roundness: 3 };
export const darkTheme: MD3Theme = { ...MD3DarkTheme, roundness: 3 };
