import { EmptyState, ScreenContainer } from '@/layout';

export function PlanningScreen() {
  return (
    <ScreenContainer>
      <EmptyState title="Planificación" description="Esta sección se construye en la Fase 6." />
    </ScreenContainer>
  );
}
